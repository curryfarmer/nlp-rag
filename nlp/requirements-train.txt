# Training-only deps. NOT in the inference Docker image.
#
# torch is NOT pinned here — install it FIRST with the right CUDA for your GPU:
#   RTX 5090 / Blackwell (sm_120): pip install torch --index-url https://download.pytorch.org/whl/cu128
#   older CUDA 12.1 GPUs:          pip install torch --index-url https://download.pytorch.org/whl/cu121
# A wrong/CPU torch wheel => "no kernel image for sm_120" on Blackwell.
# Then: pip install -r nlp/requirements-train.txt
#
transformers>=4.46
trl>=0.12
peft>=0.13
datasets>=3.0
accelerate>=1.0
bitsandbytes>=0.45    # QLoRA 4-bit; >=0.45 needed for Blackwell/sm_120
sentencepiece
# optional, faster paths:
# unsloth             # 2x faster QLoRA, less VRAM
# vllm                # high-throughput synthetic generation (gen_synthetic.py)
# autoawq             # AWQ Q4 export (quantize.py --mode awq)
